# AGH 与 Box 共存适配文档

> 本文档记录如何将上游原版 AdGuardHome 模块改造成与 Box for Root 共存（方案A），
> 并说明上游 ADG 更新后如何按本文档重新适配。配合 `apply_fix.sh` 使用。

## 1. 背景与目标

### 1.1 冲突根因

AdGuard Home（AGH，DNS 广告过滤）与 Box for Root（VPN 代理分流）同时安装时，
原版两模块在 DNS 上互相冲突：

| 模块 | 原版行为 |
|------|---------|
| AGH | 将全部 TCP/UDP 53 重定向到自身随机端口，同时 DROP 所有 IPv6 DNS |
| Box | 劫持 53 到 mihomo 的 DNS 端口，tproxy/redir 分流 |

两者规则互相覆盖/竞争，导致部分应用断网、国内外分流异常。
根因有二：
1. **双重 DNS 劫持**：AGH 的 `iptables` 53 劫持与 Box 的 tun/TPROXY 劫持并存
2. **AGH 端口随机化**：`ProxyConfig.sh` 轮询改写 mihomo DNS 存在竞态

### 1.2 方案A（本方案）

- **AGH 只做广告过滤上游**：监听 `127.0.0.1`，默认固定 5591，也可选随机端口
- **mihomo 全权接管 DNS 与分流**：`enhanced-mode: fake-ip`，`tun.enable: true`，开启 sniffer
- **mihomo 的 nameserver 指向 AGH**：每次域名解析先经 AGH 过滤再走 DoH 上游
- 单一 DNS 链路：`应用 DNS → Box 劫持 → mihomo:1053 → AGH(过滤) → DoH 上游`

### 1.3 关键设计：BoxFix 自适应协调器

新增 `scripts/BoxFix.sh`，每 30 秒检测环境并自动收敛：

- **检测到 Box**：AGH 作为上游，读取 AGH **实际 DNS 端口**（随机或 5591）同步到
  mihomo 的 nameserver；修正 mihomo 为 fake-ip；清空 AGH 的 53 劫持
- **未检测到 Box**：AGH 随机端口独立模式，iptables 劫持 53（等同原版）
- **卸载 AGH**：`uninstall.sh` 还原 Box mihomo 配置并重启 Box，避免断网

安装顺序无关，独立使用兼容，全程无需用户干预。

## 2. 修改文件总览

| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `service.sh` | 修改 | 读取端口模式，分支设置端口；启动 BoxFix 取代 iptables/ProxyConfig |
| `customize.sh` | 修改 | 新增音量键端口模式选择；恢复配置时清理 PROXY_URL |
| `scripts/BoxFix.sh` | 新增 | 自适应协调器 + 端口同步 |
| `scripts/ModuleMOD.sh` | 修改 | 模块描述随端口模式动态更新 |
| `scripts/config.prop` | 修改 | PROXY_URL 置空 |
| `uninstall.sh` | 修改 | 卸载时还原 Box mihomo 配置 |
| `bin/AdGuardHome.yaml` | 修改 | dns 仅监听 127.0.0.1（端口由 service.sh 运行时写入） |

## 3. 详细修改说明

> 以下 diff 以原版为基准。修改后脚本均以 `#!/system/bin/sh` 运行，使用 bash 兼容语法。

### 3.1 `service.sh`

原版随机端口段：

```sh
# 动态端口随机化
R1=$((30000+RANDOM%35536)); R2=$((30000+RANDOM%35536))
sed -i "s/^\([[:space:]]*port:\) [0-9]*/\1 $R1/; s/^\([[:space:]]*address:\) 127\.0\.0\.1:[0-9]*/\1 127.0.0.1:$R2/" "$BIN_DIR/AdGuardHome.yaml"
sed -i "s/^redir_port=.*/redir_port=$R1/" "$SCRIPT_DIR/config.prop" || echo "redir_port=$R1" > "$SCRIPT_DIR/config.prop"
```

改为（读取 `.port_mode`，支持 random/fixed 两种端口）：

```sh
# 读取安装时通过音量键选择的端口模式：random=随机端口 / fixed=固定端口(5591)
PORT_MODE="fixed"
[ -f "$AGH_DIR/.port_mode" ] && PORT_MODE=$(cat "$AGH_DIR/.port_mode")
# BoxFix 协调器启动后会立即收敛：
#  - Box 存在   -> AGH 作为上游，mihomo nameserver 自动同步到 AGH 实际端口
#                  random 即随机端口(30000-65535)，fixed 即固定 5591
#  - Box 不存在 -> 随机端口独立模式，iptables 劫持 53 做广告过滤
if [ "$PORT_MODE" = "random" ]; then
    R1=$((30000+RANDOM%35536)); R2=$((30000+RANDOM%35536))
    sed -i "s/^\([[:space:]]*port:\) [0-9]*/\1 $R1/; s/^\([[:space:]]*address:\) 127\.0\.0\.1:[0-9]*/\1 127.0.0.1:$R2/" "$BIN_DIR/AdGuardHome.yaml"
    sed -i "s/^redir_port=.*/redir_port=$R1/" "$SCRIPT_DIR/config.prop" || echo "redir_port=$R1" > "$SCRIPT_DIR/config.prop"
else
    sed -i "s/^\([[:space:]]*port:\) [0-9]*/\1 5591/; s/^\([[:space:]]*address:\) 127\.0\.0\.1:[0-9]*/\1 127.0.0.1:40165/" "$BIN_DIR/AdGuardHome.yaml"
    sed -i "s/^redir_port=.*/redir_port=5591/" "$SCRIPT_DIR/config.prop" || echo "redir_port=5591" > "$SCRIPT_DIR/config.prop"
fi
```

启动附加脚本段：原版启动 `iptables.sh` 与 `ProxyConfig.sh`，改为启动 `BoxFix.sh`：

```sh
# 原版
"$SCRIPT_DIR/iptables.sh" &
"$SCRIPT_DIR/ProxyConfig.sh" &

# 新版
# BoxFix 协调器：根据 Box 是否安装自动收敛工作模式
"$SCRIPT_DIR/BoxFix.sh" &
```

### 3.2 `customize.sh`

新增音量键端口模式选择（插在"执行脚本防篡改保护"之前）。完整新增块：

```sh
# ════════════════════
# 音量键端口模式选择
# ════════════════════
KEY_FIFO=$(mktemp -u -p /dev/tmp)
KEY_LISTENER_PID=""

start_key_listener() {
    [ -n "$KEY_LISTENER_PID" ] && kill -0 "$KEY_LISTENER_PID" 2>/dev/null && return
    mkfifo "$KEY_FIFO" || return 1
    getevent -ql > "$KEY_FIFO" &
    KEY_LISTENER_PID=$!
}

stop_key_listener() {
    [ -n "$KEY_LISTENER_PID" ] && kill "$KEY_LISTENER_PID" >/dev/null 2>&1
    [ -n "$KEY_FIFO" ] && rm -f "$KEY_FIFO"
    KEY_LISTENER_PID=""
}

volume_key_detection() {
    local timeout_seconds="${1:-10}"
    local detection_result_file=$(mktemp -u -p /dev/tmp)
    (
        while read -r line; do
            if echo "$line" | grep -Eiq "(KEY_)?VOLUME ?UP|KEYCODE_VOLUME_UP" && echo "$line" | grep -Eiq "DOWN|PRESS"; then
                echo "0" > "$detection_result_file"
                exit 0
            elif echo "$line" | grep -Eiq "(KEY_)?VOLUME ?DOWN|KEYCODE_VOLUME_DOWN" && echo "$line" | grep -Eiq "DOWN|PRESS"; then
                echo "1" > "$detection_result_file"
                exit 0
            fi
        done < "$KEY_FIFO"
    ) &
    local detection_pid=$!
    (
        sleep "$timeout_seconds"
        if kill -0 "$detection_pid" 2>/dev/null; then
            kill "$detection_pid" 2>/dev/null
            echo "2" > "$detection_result_file"
        fi
    ) &
    local timeout_pid=$!
    wait "$detection_pid" 2>/dev/null
    kill "$timeout_pid" 2>/dev/null
    wait "$timeout_pid" 2>/dev/null
    [ -f "$detection_result_file" ] && { local result=$(cat "$detection_result_file"); rm -f "$detection_result_file"; return "$result"; }
    rm -f "$detection_result_file"
    return 2
}

handle_choice() {
    local question="$1"
    local choice_yes="${2:-随机端口}"
    local choice_no="${3:-固定端口}"
    local timeout_seconds="${4:-10}"
    ui_print " "
    ui_print "-----------------------------------------------------------"
    ui_print "- ${question}"
    ui_print "- [ 音量加(+) ]: ${choice_yes}"
    ui_print "- [ 音量减(-) ]: ${choice_no}"
    ui_print "- [ ${timeout_seconds}秒内未选择将默认选择: ${choice_yes} ]"
    timeout 0.1 getevent -c 1 >/dev/null 2>&1
    start_key_listener
    volume_key_detection "$timeout_seconds"
    local result=$?
    stop_key_listener
    if [ "$result" -eq 0 ]; then
        ui_print "  => 您选择了: ${choice_yes}"
        return 0
    elif [ "$result" -eq 1 ]; then
        ui_print "  => 您选择了: ${choice_no}"
        return 1
    else
        ui_print "  => 超时未选择，默认选择: ${choice_yes}"
        return 0
    fi
}

MODE_FILE="$AGH_DIR/.port_mode"
PORT_MODE="random"
if [ -d "/data/adb/box" ]; then
    if handle_choice "检测到 Box 模块，选择 DNS 端口模式？" "随机端口(自动同步给Box)" "固定端口5591(自动同步给Box)"; then
        PORT_MODE="random"
    else
        PORT_MODE="fixed"
    fi
else
    if handle_choice "未检测到 Box 模块，选择 DNS 端口模式？" "随机端口(推荐)" "固定端口"; then
        PORT_MODE="random"
    else
        PORT_MODE="fixed"
    fi
fi
echo "$PORT_MODE" > "$MODE_FILE"
i18n_print "- Port mode set to: $PORT_MODE" "- 端口模式已设置为: $PORT_MODE"
```

配置恢复逻辑中追加清理 PROXY_URL（防泄露）：

```sh
# 正在保留配置文件
if [ -f "$BACKUP_DIR/config.prop" ]; then
  i18n_print "- Preserving configuration file" "- 正在保留配置文件"
  cp -f "$BACKUP_DIR/config.prop" "$SCRIPT_DIR/"
  # 清理残留的代理订阅链接，避免泄露且本模块不管理代理订阅
  sed -i 's|^PROXY_URL=.*|PROXY_URL=""|' "$SCRIPT_DIR/config.prop"
fi
```

### 3.3 `scripts/BoxFix.sh`（新增）

核心交付物。完整内容见 `apply_fix.sh` 内嵌版本（两者保持一致）。要点：

- 每 30 秒循环检测
- `box_installed()`：`/data/adb/box` 目录与 `config.yaml` 是否存在
- `get_agh_port()`：从 `AdGuardHome.yaml` 的 dns.port 读取实际端口
- `is_synced()`：mihomo 是否已 fake-ip 且 nameserver 指向 AGH 实际端口
- `sync_mihomo()`：备份 `config.yaml.aghbak`，sed 改写 fake-ip/nameserver/tun/sniffer，删 Local_DNS_Forward，重启 box.service
- `cleanup_standalone()`：杀 ProxyConfig/iptables，清 53 劫持规则
- `switch_to_standalone()`：无 Box 时随机端口 + 重启 AGH + 启动 iptables/ProxyConfig

### 3.4 `scripts/ModuleMOD.sh`

描述随端口模式动态更新。改动点：
1. 顶部新增 `MODE_FILE="/data/adb/agh/.port_mode"` 与 `PORT_MODE` 读取
2. 判断条件增加 `[ "$PORT_MODE" != "$LAST_MODE" ]`，设置 `LAST_MODE="$PORT_MODE"`
3. 描述文案按 random/fixed 分支（中英文），见 `apply_fix.sh` 对应逻辑

### 3.5 `scripts/config.prop`

```sh
# 原版
PROXY_URL="https://...订阅token..."

# 新版
PROXY_URL=""
```

### 3.6 `uninstall.sh`

在脚本开头（`#!/system/bin/sh` 之后、`pkill -9 "ProxyConfig"` 附近）追加：

```sh
pkill -9 "BoxFix"

# 还原 Box 的 mihomo 配置并重启 Box 服务（防止 Box 继续指向已卸载的 5591 导致断网）
BOX_CFG="/data/adb/box/mihomo/config.yaml"
BOX_BAK="$BOX_CFG.aghbak"
BOX_SERVICE="/data/adb/box/scripts/box.service"
if [ -f "$BOX_BAK" ]; then
    cp -f "$BOX_BAK" "$BOX_CFG"
    rm -f "$BOX_BAK"
    [ -f "$BOX_SERVICE" ] && "$BOX_SERVICE" restart
    echo "$(date '+%F %T') [uninstall] 已还原 Box mihomo 配置并重启服务" >> "$AGH_DIR/agh.log" 2>/dev/null || true
fi
```

### 3.7 `bin/AdGuardHome.yaml`

将 dns 块改为仅监听 127.0.0.1（端口由 service.sh 启动时写入，固定值以 5591 为默认）：

```yaml
dns:
  bind_hosts:
    - 127.0.0.1
  port: 5591
```

## 4. 打包方法

```bash
# 以修复后的模块目录（含 META-INF/customize.sh 等）打包
cd <模块目录>
zip -r ../AdGuard.Home.For.Android.20260720-fixed.zip . -x "./.port_mode"
```

检查包内关键文件齐全：
- `customize.sh` / `service.sh` / `uninstall.sh` / `action.sh` / `module.prop`
- `scripts/`（BoxFix.sh、ModuleMOD.sh、NoAdsService.sh、ProxyConfig.sh、iptables.sh、config.prop）
- `bin/`（AdGuardHome 可执行文件、AdGuardHome.yaml、data/）

## 5. 上游 ADG 更新后的适配流程

### 方式一：一键脚本（推荐）

1. 重刷上游新版 ADG 包（不要用本仓库 fixed 包）
2. 将 `apply_fix.sh` push 到设备
3. root 终端执行：

```bash
sh /data/local/tmp/apply_fix.sh
```

自动完成：写入 BoxFix.sh、修改 service.sh 启动 BoxFix、修改 uninstall.sh 还原、
清空 PROXY_URL、重新锁定脚本。

**已知限制**：apply_fix.sh 不恢复 customize.sh 的音量键选择功能（需手动按 3.2 节修改）。

### 方式二：手动按本文档修改

按第 3 节逐文件修改后按第 4 节打包。

### 方式三：交给仓库维护者

将上游新版 zip 交给维护者，维护者重新打包后发布到本仓库。

## 6. 验证

### 检测到 Box（随机或固定端口共存）
```bash
iptables -t nat -L ADGUARD        # 应为空（无 53 劫持）
grep -E "port|bind_hosts" /data/adb/agh/bin/AdGuardHome.yaml   # DNS 监听 127.0.0.1
grep -E "enhanced-mode|127.0.0.1" /data/adb/box/mihomo/config.yaml  # fake-ip + 指向 AGH 实际端口
cat /data/adb/agh/.port_mode      # random 或 fixed
```

### 未检测到 Box（随机端口独立）
```bash
iptables -t nat -L ADGUARD        # 有 53 REDIRECT 规则
grep redir_port /data/adb/agh/scripts/config.prop
```

## 7. 回滚

- **共存模式**：将 `config.yaml.aghbak` 还原为 `/data/adb/box/mihomo/config.yaml`，
  重刷原版 AGH 包
- **独立模式**：重刷原版 AGH 包即可
- 删除 `/data/adb/agh/.port_mode` 后重刷模块可重新通过音量键选择模式

## 8. 隐私安全

- `config.prop` 中 `PROXY_URL` 必须为空（本模块不管理代理订阅）
- Box 的 `config.yaml` 中订阅 url 应使用占位符，勿提交真实订阅 token
- 本文档与发行包均不含真实订阅信息
